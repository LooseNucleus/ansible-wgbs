--- 
sudo: required 

language: python

python: 
  - "2.7"

env: 
  global: 
    - ANSIBLE_CONFIG=""${PWD}"/tests/ansible.cfg"
  matrix:
    - distribution: ubuntu
      version: trusty

services:
  - docker

before_install:
  - 'sudo docker pull ${distribution}:${version}'
  - 'sudo docker build --no-cache --rm --file=travis/Dockerfile.${distribution}-${version} --tag=${distribution}-${version}:ansible travis'

install: 
  - pip install ansible==2.4.3.0


script:
  - container_id=$(mktemp)
  - 'sudo docker run --detach --privileged -v /sys/fs/cgroup:/sys/fs/cgroup -p 127.0.0.1:1234:22 ${distribution}-${version}:ansible > "${container_id}"'
  - echo "${PWD}"
  - echo $ANSIBLE_CONFIG
  - ansible-galaxy install coadunate.devtools -p "${PWD}"/roles
  - ansible-config dump
  - env ANSIBLE_CONFIG="${PWD}"/tests/ansible.cfg; ansible-playbook -i tests/inventory tests/test.yml
  
  - 'sudo docker rm -f "$(cat ${container_id})"'
